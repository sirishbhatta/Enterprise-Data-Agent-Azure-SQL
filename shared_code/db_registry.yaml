databases:
  azure_sql:
    name: Azure SQL
    type: mssql
    host: sirish-azure-sql-server.database.windows.net
    database: sirish_azure_sql_db
    username_env: SQL_USER
    password_env: SQL_PASSWORD
    domain: AZURE_SQL
    enabled: true
    description: Azure SQL cloud database — primary data store
